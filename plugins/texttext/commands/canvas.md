---
description: Use one Texttext document as a live canvas for the current task
---

Use the Texttext MCP tools and the `live-document` skill.

Find the durable document for this project or purpose, or create it once if it
does not exist. Open the exact item in edit mode. On macOS, launch the
`native_url` returned by `open_item` so Texttext opens directly at the correct
workspace, folder, and document. Keep that same item current while we work,
show the agent identity and live edits, preserve concurrent human edits, and
use stable idempotency keys for retryable appends.

Task or document context:

$ARGUMENTS
