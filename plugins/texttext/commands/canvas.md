---
description: Use one TextText document as a live canvas for the current task
---

Use the `live-document` skill and the local `texttext` command when it is
available. The skill resolves the command from PATH or the canonical TextText
app bundle and verifies it with a read before changing anything. Do not start an
MCP server or ask for a workspace token for local work.

Find the durable document for this project or purpose, or create it once if it
does not exist. Open the exact item in TextText. Keep that same item current
while we work, show the agent identity and live edits, and preserve concurrent
human edits. Use hosted MCP only if it is already connected and the local
command is unavailable.

Task or document context:

$ARGUMENTS
