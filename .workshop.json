{
  "schema": "workshop-project-v1",
  "id": "texttext",
  "displayName": "TextText.app",
  "defaultProfile": "quick",
  "profiles": {
    "quick": {
      "description": "Install, typecheck, and unit tests without production services",
      "command": "npm ci && npx tsc --noEmit && npm test",
      "timeoutMinutes": 45
    },
    "full": {
      "description": "Portable typecheck, tests, and production build",
      "command": "npm ci && npx tsc --noEmit && npm test && npm run build",
      "timeoutMinutes": 90
    }
  }
}
